[pytest]
testpaths = test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Pure unit tests (no ROS2 required)
    node: Tests that spin up ROS2 nodes
    integration: Full integration tests
